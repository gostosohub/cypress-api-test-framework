/// <reference types="cypress" />

describe('Test login endpoint /login', () => {

  it('Verify if the user can log in with valid credentials', () => {
        // return 200
  })

  it('Verify if the user cannot login if credentials are invalid', () => {
        // return 401 ( unauthorised )
  })

  it('Verify if the user cannot login if credentials are empty', () => {
      // return 401 ( unauthorised )
  })

})
